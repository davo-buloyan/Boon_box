export interface CardModel {
    categoryId: number;
    categoryName: string;
    images: string[];
  }

  export class CardModel {
    categoryId: number;
    categoryName: string;
    images: string[];
  }